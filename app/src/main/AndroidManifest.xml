<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/mipmaphdpi"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/mipmaphdpi"
        android:supportsRtl="true"
        android:theme="@style/Theme.Juegos"
        tools:targetApi="31" >
        <activity
            android:name=".activities.LineFourActivity"
            android:exported="false"
            android:theme="@style/Theme.Juegos" />
        <activity
            android:name=".activities.MainActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.MainGameActivity"
            android:exported="true" >
        </activity>
        <activity
            android:name=".activities.UserInfoActivity"
            android:exported="true" >
        </activity>
        <activity
            android:name=".activities.SettingsActivity"
            android:exported="true" >
        </activity>

    </application>

</manifest>